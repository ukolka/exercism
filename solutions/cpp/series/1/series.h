#if !defined(SERIES_H)
#define SERIES_H
#include <vector>
#include <string>
using namespace std;

namespace series {
    vector<string> slice(const string&, size_t);

}  // namespace series

#endif // SERIES_H