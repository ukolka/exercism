#if !defined(FOOD_CHAIN_H)
#define FOOD_CHAIN_H
#include <string>
using namespace std;

namespace food_chain {
    string verse(int n);
    string verses(int start, int end);
    string sing();
}  // namespace food_chain

#endif // FOOD_CHAIN_H